server:
  port: 8080
spring:
  datasource:
    url: jdbc:p6spy:mysql://localhost:3306/suxy?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Su@251485
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver

  redis:
    # Redis地址
    host: 127.0.0.1
    # Redis端口
    port: 6379
    # 数据库
    database: 0
    # 连接超时时间
    timeout: 10000
  redisson:
    # 线程池配置
    threads: 5
    netty-threads: 5


mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: classpath:mapper/*.xml # 你的 XML 文件存放路径
  type-aliases-package: org.sfc.sfc.entity # 实体类包路径

logging:
  level:
    p6spy: info